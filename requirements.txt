# CitateGenie Requirements
# Updated: 2025-12-17 - Added billing, auth, database

# Web Framework
flask>=2.3.0
gunicorn>=21.0.0

# HTTP Client
requests>=2.28.0

# Document Processing
python-docx>=0.8.11

# HTML Parsing (for URL metadata extraction)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# AI APIs (tiered fallback: GPT-4o → Claude)
openai>=1.0.0
anthropic>=0.18.0
google-generativeai>=0.3.0

# Security/Encryption (for SOC 2 compliance)
cryptography>=41.0.0

# Database (PostgreSQL + ORM)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Authentication
flask-login>=0.6.0
bcrypt>=4.0.0

# Payment Processing
stripe>=7.0.0
